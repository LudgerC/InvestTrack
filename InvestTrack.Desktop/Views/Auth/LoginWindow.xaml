<Window x:Class="InvestTrack.Desktop.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="260"
        Width="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFFDFDFD">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Invoervelden -->
        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="E-mail" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="EmailBox"
                     Height="28"
                     Margin="0,0,0,8"
                     Padding="4"/>

            <TextBlock Text="Wachtwoord" FontWeight="SemiBold" Margin="0,8,0,4"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="28"
                         Padding="4"/>
        </StackPanel>

        <!-- Knoppen -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Login"
                    Width="90"
                    Margin="4,0"
                    Click="Login_Click"/>
            <Button Content="Register"
                    Width="90"
                    Margin="4,0"
                    Click="OpenRegister_Click"/>
        </StackPanel>

        <!-- Foutmelding -->
        <TextBlock x:Name="ErrorText"
                   Grid.Row="2"
                   Foreground="Red"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"/>
    </Grid>
</Window>
